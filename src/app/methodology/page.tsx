import Link from "next/link";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Methodology | GovernIQ",
  description: "How GovernIQ scores governance maturity across seven constructs using illustrative Bursa Malaysia data.",
};
import { ArrowLeft, CheckCircle2, ShieldCheck } from "lucide-react";
import { Card, CardContent } from "@/components/ui/card";
import { BENCHMARK_AVERAGES, CONSTRUCT_LABELS, CONSTRUCT_KEYS } from "@/types/company";

export default function MethodologyPage() {
  return <div className="app-shell min-h-screen bg-zinc-950 text-zinc-100"><header className="sticky top-0 z-20 border-b border-white/10 bg-zinc-950/55 backdrop-blur-xl"><div className="mx-auto max-w-4xl px-4 py-5"><Link href="/" className="inline-flex items-center gap-2 text-sm text-zinc-400 hover:text-white"><ArrowLeft className="h-4 w-4" /> Back to overview</Link><div className="mt-8 flex items-center gap-3"><ShieldCheck className="h-7 w-7 text-emerald-400" /><div><p className="text-xs uppercase tracking-[0.2em] text-zinc-500">GovernIQ / evidence layer</p><h1 className="text-2xl font-semibold">Methodology & boundaries</h1></div></div></div></header><main className="mx-auto max-w-4xl px-4 py-10"><Card className="border-emerald-500/20 bg-emerald-500/[0.06]"><CardContent className="p-6"><p className="text-sm leading-relaxed text-zinc-200">GovernIQ is a decision-support prototype. The current company universe and market reference values are illustrative demo fixtures, not live or validated data. They must not be used as investment advice, compliance certification, or legal advice.</p></CardContent></Card><section className="mt-8"><h2 className="text-2xl font-semibold">The seven-construct model</h2><p className="mt-2 text-sm leading-relaxed text-zinc-500">The model organises 35 indicators into seven explainable governance dimensions. Each dimension can be replaced or recalibrated when a validated source is connected.</p><div className="mt-5 grid gap-3 sm:grid-cols-2">{CONSTRUCT_KEYS.map((key) => <Card key={key} className="glass-card"><CardContent className="p-5"><div className="flex items-center justify-between"><h3 className="font-medium">{CONSTRUCT_LABELS[key]}</h3><span className="text-sm tabular-nums text-emerald-300">{BENCHMARK_AVERAGES[key]}/100</span></div><p className="mt-2 text-xs leading-relaxed text-zinc-500">A structured lens for governance practice, evidence quality, accountability, and stakeholder confidence.</p></CardContent></Card>)}</div></section><section className="mt-10"><h2 className="text-2xl font-semibold">How scoring works</h2><div className="mt-5 grid gap-3 sm:grid-cols-3">{[["01", "Indicator", "Each maturity response uses a 1–5 scale from General to Leading Practice."], ["02", "Construct", "Indicator values are converted to 0–100 and averaged within each construct."], ["03", "Overall", "The seven construct scores are averaged into an overall 0–100 score."]].map(([n, title, body]) => <div key={n} className="rounded-lg border glass-card p-5"><span className="text-xs text-emerald-400">{n}</span><h3 className="mt-3 font-medium">{title}</h3><p className="mt-2 text-sm leading-relaxed text-zinc-500">{body}</p></div>)}</div><div className="mt-5 grid gap-3 sm:grid-cols-3">{[["Critical", "Below 50", "Immediate evidence and ownership gap"], ["Watch", "50–74", "Developing practice needing consistency"], ["Strong", "75–100", "Practice to maintain and evidence"]].map(([title, range, body]) => <div key={title} className="border-l-2 border-emerald-500/60 pl-4"><p className="font-medium">{title} <span className="ml-2 text-xs text-zinc-500">{range}</span></p><p className="mt-1 text-xs text-zinc-500">{body}</p></div>)}</div></section><section className="mt-10 grid gap-4 sm:grid-cols-2"><Card className="glass-card"><CardContent className="p-6"><h2 className="font-semibold">Evidence boundaries</h2><div className="mt-4 space-y-3 text-sm text-zinc-400">{["Demo fixtures are labelled and not a live dataset.", "A score is a signal, not proof of compliance or performance.", "Reference-period and methodology changes can affect comparability.", "Users should inspect source evidence before making material decisions."].map((x) => <p key={x} className="flex gap-2"><CheckCircle2 className="mt-0.5 h-4 w-4 shrink-0 text-emerald-400" />{x}</p>)}</div></CardContent></Card><Card className="glass-card"><CardContent className="p-6"><h2 className="font-semibold">What comes next</h2><p className="mt-3 text-sm leading-relaxed text-zinc-500">A production benchmark should document its source dataset, reference period, sampling frame, evidence provenance, validation tests, and change log before being used for external reporting.</p><Link href="/readiness" className="mt-5 inline-flex items-center text-sm text-emerald-400 hover:text-emerald-300">Run a session-only readiness review <span className="ml-2">→</span></Link></CardContent></Card></section></main></div>;
}
